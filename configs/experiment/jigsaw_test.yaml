# @package _global_


module:
    model:
        num_labels: 1
        pretrained_model_name_or_path: distilbert-base-uncased
        
datamodule:
    batch_size: 32
    path_to_data: ${jigsaw}/train_big.csv
    
trainer:
    gpus: 1
    val_check_interval: 1.0

